<div class="container">
  <div class="flex flex-col items-center mt-8 md:mt-48">
    <%= tag.h2 "Seja bem vindo(a), #{@doctor.name}!" if @doctor.user == current_user %>

    <div class="mt-8 w-full flex justify-center">
      <%= render "shared/presentation_card", record: @doctor %>
    </div>
  </div>
</div>
